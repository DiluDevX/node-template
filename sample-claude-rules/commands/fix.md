---
description: Auto-fix lint and format issues in the codebase
---

Run these commands sequentially and report results:

1. `yarn format:fix` — fix Prettier formatting
2. `yarn lint:fix` — fix ESLint issues
3. `yarn typecheck` — verify no type errors remain

Report which files were changed and any remaining issues that need manual fixing.

$ARGUMENTS
